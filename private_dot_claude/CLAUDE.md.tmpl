## Git Commits

Follow conventional commits when writing git commits.

## PR Comments

<pr-comment-rule>
When I say to add a comment to a PR with a TODO on it, use 'checkbox' markdown format to add the TODO. For instance:

<example>
- [ ] A description of the TODO goes here
</example>
</pr-comment-rule>
- When tagging Claude in GitHub issues, use '@claude'

## Change sets

To add a change set, write a new file to the `.changeset` directory.

The file should be named `0000-your-change.md`. Decide yourself whether to make it a patch, minor, or major change.

The format of the file should be:

```md
---
Relevant front-matter
---


Description of the change.
```

## GitHub

- Your primary method for interacting with GitHub should be the GitHub CLI.

## Git

- When creating branches, prefix them with `AbysmalBiscuit-claude/` to indicate they came from me and claude.

## Plans

- At the end of each plan, give me a list of unresolved questions to answer, if any.

## ALWAYS START WITH THESE COMMANDS FOR COMMON TASKS

{{- if or (index .tool "fd" | default false) (index .tool "rg" | default false) }}

### Preferred Command-Line Tools

This machine has modern alternatives to traditional Unix tools installed, ALWAYS prefer using them over the traditional ones unless there's a strong reason not to.

#### Search Tools

{{- if index .tool "rg" | default false }}

- **ripgrep (`rg`)** instead of `grep`:
- Faster recursive search with smart defaults
- Automatically respects `.gitignore`
- Example: `rg "pattern"` instead of `grep -r "pattern"`
{{- end }}

{{- if index .tool "fd" | default false }}

- **fd** instead of `find`:
- Faster file search with intuitive syntax
- Respects `.gitignore` by default
- Example: `fd "filename"` instead of `find . -name "filename"`
{{- end }}

**Data processing**:
{{- if index .tool "jq" }}
- `jq` - JSON processor
{{- end }}
{{- if index .tool "yq" }}
- `yq` - YAML processor (same syntax as jq)
{{- end }}

**Search & selection**:
{{- if index .tool "rg" }}
- `rg` (ripgrep) - fast grep
{{- end }}
{{- if index .tool "fd" }}
- `fd` - fast find
{{- end }}

**System**:
- `hyperfine` - benchmarking
{{- if index .tool "watchexec" }}
- `watchexec` - run commands when files change (e.g., `watchexec "COMMAND"`)
{{- end }}

#### Tools for Specific Tasks

**Task: "List/summarize all files and directories"**

```bash
{{- if index .tool "fd" | default false }}
fd [PATTERN] -t f           # Lists ALL files recursively (FASTEST), optional PATTERN
{{- end }}
{{- if and (index .tool "fd" | default false) (index .tool "rg" | default false) }}
# OR
{{- end }}
{{- if index .tool "rg" | default false }}
rg --files PATTERN         # Lists files (respects .gitignore), optional PATTERN
{{- end }}
```

{{- if index .tool "rg" | default false }}

**Task: "Search for content in files"**

```bash
rg "PATTERN"    # Search in all files (FASTEST)
```
{{- end }}


{{- if index .tool "fd" | default false }}

**Task: "Find files by name"**

```bash
fd "filename"       # Find by name pattern (FASTEST)
```
{{- end }}

### Directory/File Exploration

```bash
# FIRST CHOICE - List all files/dirs recursively:
{{- if index .tool "fd" | default false }}
fd PATTERN -t f           # All files (fastest)
fd PATTERN -t d           # All directories
{{- end }}
{{- if index .tool "rg" | default false }}
rg --files          # All files (respects .gitignore)
{{- end }}

# For current directory only:
ls -la              # OK for single directory view
```

### BANNED - Never Use These Slow Tools

{{- if and (index .tool "fd" | default false) (index .tool "rg" | default false) }}
* ❌ `fd . PATH | rg PATTERN` - use `fd PATTERN` or `rg --files PATTERN`
* ❌ `fd . PATH | grep PATTERN` - use `fd PATTERN` or `rg --files PATTERN`
* ❌ `find` - use `fd` or `rg --files`
* ❌ `ls -R` - use `fd` or `rg --files`
{{- else if index .tool "fd" | default false -}}
* ❌ `find` - use `fd`
* ❌ `ls -R` - use `fd`
{{- else if index .tool "rg" | default false -}}
* ❌ `find` - use `rg --files`
* ❌ `ls -R` - use `rg`
{{- end }}
{{- if index .tool "rg" | default false }}
* ❌ `grep` or `grep -r` - use `rg` instead
* ❌ `cat file | grep` - use `rg pattern file`
{{- end }}
{{- end }}

### Use These Faster Tools Instead

```bash
{{- if index .tool "rg" | default false }}
# ripgrep (rg) - content search
rg "search_term"                # Search in all files
rg -i "case_insensitive"        # Case-insensitive
rg "pattern" -t py              # Only Python files
rg "pattern" -g "*.md"          # Only Markdown
rg -1 "pattern"                 # Filenames with matches
rg -c "pattern"                 # Count matches per file
rg -n "pattern"                 # Show line numbers
rg -A 3 -B 3 "error"            # Context lines
rg " (TODO|FIXME|HACK)"      # Multiple patterns

# ripgrep (rg) - file listing
rg --files                      # List files (respects •gitignore)
rg --files | rg "pattern"       # Find files by name
rg --files -t md                # Only Markdown files
{{- end }}

{{- if index .tool "fd" | default false }}
# fd - file finding
fd -e js                        # All •js files (fast find)
fd -x command {}                # Exec per-file
fd -e md -x ls -la {}           # Example with ls
{{- end }}

{{- if index .tool "jq" | default false }}
# jq - JSON processing
jq. data.json                   # Pretty-print
jq -r .name file.json           # Extract field
jq '.id = 0' x.json             # Modify field
{{- end }}
```

{{- if index .tool "rg" | default false }}

### Search Strategy

1. Start broad, then narrow: `rg "partial" | rg "specific"`
2. Filter by type early: `rg -t python "def function_name"`
3. Batch patterns: `rg "(pattern1|pattern2|pattern3)"`
4. Limit scope: `rg "pattern" src/`
{{- end }}

### INSTANT DECISION TREE

```
{{- if and (index .tool "fd" | default false) (index .tool "rg" | default false) }}
User asks to "list/show/summarize/explore files"?
    → USE: fd . -t f  (fastest, shows all files)
    → OR: rg --files  (respects .gitignore)
{{- end }}

{{- if index .tool "rg" | default false }}

User asks to "search/grep/find text content"?
    → USE: rg "pattern"  (NOT grep!)
{{- end }}

{{- if index .tool "fd" | default false }}
User asks to "find file/directory by name"?
    → USE: fd "name"  (NOT find!)

User asks for "directory structure/tree"?
    → USE: fd . -t d  (directories) + fd . -t f  (files)
{{- end }}

Need just current directory?
    → USE: ls -la  (OK for single dir)
```
