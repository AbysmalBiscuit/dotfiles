# chezmoi:template:left-delimiter="# {{" right-delimiter="}}"
# {{- if true -}}
# vim:filetype=zsh:set shiftwidth=4:
# {{- else -}}
# vim:filetype=zsh.chezmoitmpl:set shiftwidth=4:
# {{- end }}

# if [[ $- != *i* ]] && [[ -f "$HOME/.cache/zsh/cache_env.zsh" ]]; then
# source "$HOME/.cache/zsh/cache_env.zsh"
# return 0
# fi

# {{ template "sh/utility_functions.sh" }}

# {{ if .cache.build.shell -}}
[[ -s "$HOME/.sh_cache" ]] && source "$HOME/.sh_cache"
# {{- else -}}
[[ -s "$HOME/.sh_env" ]] && source "$HOME/.sh_env"
# {{- end }}
