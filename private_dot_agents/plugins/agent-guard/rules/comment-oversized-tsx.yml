id: comment-oversized-tsx
language: tsx
severity: warning
message: "inline comment block over 12 lines; if the code needs a paragraph of justification, fix the code"
rule:
  kind: comment
  all:
    - regex: '(?s)^(.*\n){12,}'
    - not:
        regex: '^/\*\*'
