{"expectations":[{"text":"Explains git-stack does NOT resolve conflicts and hands control back to plain git","passed":true,"evidence":"'git-stack does not resolve merge conflicts ... it stops and hands control back to plain git rebase'."},{"text":"Walks through resolving with git (edit files, git add, git rebase --continue), or git rebase --abort to bail","passed":true,"evidence":"Step 2a: edit markers, git add, git rebase --continue; Step 2b: git rebase --abort."},{"text":"Mentions that after a manual rebase the stack may need `git-stack --repair`, or that branch-stash apply can return to the pre-sync state","passed":true,"evidence":"Step 3: 'git-stack --repair' to re-knit split/diverged branches; also branch-stash apply git-stack restores pre-sync positions."},{"text":"If it mentions branch-stash as the bail-out, targets the `git-stack` stash stack by name, not the default","passed":true,"evidence":"'git branch-stash apply git-stack' / 'pop git-stack', explicitly warning the default 'recent' stack is wrong."}],"score":1.0,"summary":"All four satisfied, including correct git-stack --repair and named branch-stash bail-out."}
